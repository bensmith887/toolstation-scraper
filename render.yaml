services:
  - type: web
    name: toolstation-scraper
    env: node
    plan: free
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: API_KEY
        generateValue: true
      - key: NODE_ENV
        value: production
